# Day 21 – TLS / SSL: The Lock Behind Every Secure Website

## Core idea
TLS is the trust layer of the internet.
It secures data while it is moving between a client and a server.

TLS is what makes HTTPS actually secure.

## What this day covers
- Where TLS lives in the network stack
- SSL vs TLS (real truth)
- TLS handshake (step-by-step)
- What TLS actually guarantees
- TLS versions and what to use today
- Certificates and their types
- Common myths about TLS
- Where TLS is mandatory

See `NOTES.md` for detailed explanation.